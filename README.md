# Coach a Coach

Global Peer Coaching Exchange operations prototype.

## Preview the mockup

Open `index.html` directly in a browser, or serve the folder locally:

```bash
python3 -m http.server 4173
```

Then visit `http://localhost:4173`.

The prototype uses fictional data and does not connect to Google Sheets or OpenAI.
